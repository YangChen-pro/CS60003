# 项目状态

主线目标：完成 HW1 的 EuroSAT 三层 MLP 作业代码，实现训练、评估、超参数搜索与可视化，并在远端环境完成同步运行验证。

正在做什么：`hw1/` 的实现、README 和知识库已完成，本地 smoke test 已通过，正在准备 Git 提交、推送与远端同步验证。

关键上下文：
- 主实现使用手写反向传播，`CuPy` 仅作为数组后端
- 所有 HW1 代码必须保留在 `hw1/` 目录
- 训练完成后需要推送 GitHub，再到远端 `/data/yc/CS60003` 先 `git pull` 后运行

下一步：执行 Git 提交并推送到 GitHub，然后连接远端 `/data/yc/CS60003` 拉取最新代码，用 `llm-26-gpu` 环境做 `CuPy` smoke test。

阻塞项：
- 远端验证尚未执行
- GitHub 推送尚未执行
