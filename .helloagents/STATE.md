# 项目状态

主线目标：完成 HW1 的 EuroSAT 三层 MLP 作业代码，实现训练、评估、超参数搜索与可视化，并在远端环境完成同步运行验证。

正在做什么：HW1 实现、GitHub 推送和远端同步验证都已完成，当前处于收尾完成状态。

关键上下文：
- 主实现使用手写反向传播，`CuPy` 仅作为数组后端
- 所有 HW1 代码必须保留在 `hw1/` 目录
- 训练完成后需要推送 GitHub，再到远端 `/data/yc/CS60003` 先 `git pull` 后运行

下一步：当前任务已完成；若继续推进，可基于 `hw1/outputs/` 中的图表和指标撰写实验报告，并补充最终模型权重下载链接。

阻塞项：
- 无
