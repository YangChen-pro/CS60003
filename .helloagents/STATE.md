# 项目状态

主线目标：继续在作业边界内优化 HW1 三层 MLP，并将最新最优正式模型同步到本地、远端与 GitHub。

正在做什么：dropout 优化已完成，正在把 `final_p` 正式模型及其图表、报告和配置同步收口。

关键上下文：
- 主实现使用手写反向传播，`CuPy` 仅作为数组后端
- 所有 HW1 代码必须保留在 `hw1/` 目录
- 训练完成后需要推送 GitHub，再到远端 `/data/yc/CS60003` 先 `git pull` 后运行
- 正式提交模型更新为 `best`/`final_p`：`1280 -> 768`, `dropout=0.15`, `44 epochs`, `val_acc=0.6901`, `test_acc=0.6758`
- 扩展上限实验更新为 `final_o`：`test_acc=0.6810`，但验证集略低于正式提交模型
- 远端直连 GitHub 当前不稳定，已采用 `git format-patch | git am` 作为不走 `scp` 的 Git 兜底同步方式

下一步：提交本地最终配置与报告更新，并用 Git 补丁同步到远端。

阻塞项：
- 无
