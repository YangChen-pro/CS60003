# 项目状态

主线目标：完成 HW1 的 EuroSAT 三层 MLP 作业代码、正式实验、简写实验报告与最终同步交付。

正在做什么：当前主线已完成，代码、报告、图表、GitHub 和远端仓库都已同步到最终提交。

关键上下文：
- 主实现使用手写反向传播，`CuPy` 仅作为数组后端
- 所有 HW1 代码必须保留在 `hw1/` 目录
- 训练完成后需要推送 GitHub，再到远端 `/data/yc/CS60003` 先 `git pull` 后运行
- 正式提交模型为 `best`/`final_a`：`1280 -> 768`，`val_acc=0.6849`，`test_acc=0.6669`
- 扩展上限实验 `final_c` 达到 `test_acc=0.6748`，但不作为正式提交模型

下一步：当前任务已完成；如需继续冲性能，可在现有三层 MLP 约束下继续尝试更宽隐藏层和更长训练。

阻塞项：
- 无
